source "https://rubygems.org"

# GitHub Pages와 동일한 Jekyll 환경(3.10.x + jekyll-feed + jekyll-seo-tag 포함).
# Vercel 빌드와 기존 Pages 빌드의 렌더링을 일치시키기 위해 github-pages 젬을 쓴다.
gem "github-pages", group: :jekyll_plugins
